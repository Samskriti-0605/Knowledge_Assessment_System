services:
  # Backend: PHP + Apache (Docker)
  - type: web
    name: knowledge-backend
    runtime: docker
    dockerfilePath: Dockerfile
    rootDir: backend
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: knowledge-db
          property: connectionString

  # Frontend: React (Static Site)
  - type: web
    name: knowledge-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        fromService:
          name: knowledge-backend
          type: web
          property: host

databases:
  - name: knowledge-db
    plan: free
